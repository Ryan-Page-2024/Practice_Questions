# ID: 10012
# Company: Uber

# Question: 
Find effectiveness of each advertising channel in the period from 2017 to 2018 (included). 
The effectiveness is calculated as the ratio of total money spent to total customers aquired.

# Import your libraries
import pandas as pd

# Start writing code
df = uber_advertising.loc[uber_advertising['year'].between(2017,2018), ]
df = (
    df.groupby('advertising_channel')
    .agg({'money_spent':'sum', 'customers_acquired':'sum'})
    .reset_index())
df['ratio'] = df['money_spent'] / df['customers_acquired']
df = df.loc[:,['advertising_channel','ratio']]
