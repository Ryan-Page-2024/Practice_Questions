# ID: 9622
# Company: Airbnb

# Question:
Find the average number of bathrooms and bedrooms for each city’s property types. Output the result along with the city name and the property type.

# Solution
import pandas as pd
df = airbnb_search_details.loc[:, ['city','property_type','bathrooms','bedrooms']]
.agg({'bathrooms':'mean', 'bedrooms':'mean'})
.rename(columns={'bathrooms':'avg_bath', 'bedrooms':'avg_bed'})
.reset_index()
